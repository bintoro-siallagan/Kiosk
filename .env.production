# karyaOS — Production build env (Vite priority overrides .env)
# VITE_API_URL DELIBERATELY EMPTY untuk paksa same-origin di browser HTTPS.
# Jangan set ke LAN IP — akan trigger Mixed Content block.
VITE_API_URL=
VITE_TRACKING_BASE_URL=
